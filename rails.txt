RVM:
	rvm list
	rvm gemset list
	rvm install 1.9.3
	rvm use 1.9.3@nom --create --default
	rvm gemset create nom
	rvm gemset delete nom
	rvm use ruby-1.9.3-p362@nom

	rvm --rvmrc ruby-1.9.3-p194@first_project

Commands:
	bundle exec rails s
	bundle exec rails console --sandbox
Generate code:
	rails new nom_app --skip-test-unit
	bundle update
	bundle install --without production

	rails generate controller StaticPages home help --no-test-framework
	rails generate integration_test static_pages
	rails generate controller Users new --no-test-framework
	rails generate integration_test user_pages
	
	rails generate model User name:string email:string

	rails generate scaffold User name:string email:string

Database:
	#Annotation 
		gem 'annotate'
		bundle exec annotate
		bundle exec annotate --position before
		annotate --exclude tests,fixtures,factories
		annotate --routes
		annotate --delete
		annotate --routes --delete
	#Migration Commetns
		gem 'migration_comments', :git => 'https://github.com/pinnymz/migration_comments.git'

	bundle exec rake db:migrate
	bundle exec rake db:test:prepare

	rails generate migration add_index_to_users_email
	
	rails generate migration add_password_digest_to_users password_digest:string

Test:
	rails generate rspec:install
	rails generate integration_test static_pages

	bundle exec rspec spec/requests/static_pages_spec.rb

	#Automated test with guard:
		gem 'guard-rspec', '1.2.1'
		bundle exec guard init rspec
		
		bundle exec guard
	#Speed up tests with Spork
		gem 'guard-spork', '1.2.0'
  		gem 'spork', '0.9.2'
		bundle exec spork --bootstrap
		spec/spec_helper.rb
		time bundle exec rspec spec/requests/static_pages_spec.rb --drb
		.rspec add --drb
		bundle exec guard init spork #Update GuardFile

	#Cucumber
		gem 'cucumber-rails', '1.2.1', :require => false
  		gem 'database_cleaner', '0.7.0'
		rails generate cucumber:install

		bundle exec cucumber features/
		bundle exec rake cucumber
Git:
	git init
	git add .
	git commit -m "Initial commit"

	.gitignore
	git rm --cached filename #untrack file

	#Branch & Merge
	git checkout -b static-pages #New branch
	git checkout master
	git merge static-pages
GitHub:
	git remote add origin git@github.com:<username>/sample_app.git
	git push -u origin master
	git push
Heroku:
	heroku create --stack cedar
	git push heroku master
	git push heroku
	heroku logs
